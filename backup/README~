my vimrc used across windows and linux
## Install
### Linux
backup your .vim and .vimrc
  cp ~/.vimrc ~/.vim && mv ~/.vim ~/.vim_old
git clone to .vim
  git clone https://github.com/ghostylee/dotVim.git ~/.vim
  ln -sfn ~/.vim/vimrc .vimrc
### Windows
backup your ~/.vim(if you got one)
  mv ~/.vim ~/.vim_old
git clone to .vim
  git clone https://github.com/ghostylee/dotVim.git ~/.vim
add this to $VIMRUNTIME/_vimrc
  source ~/.vim/vimrc
